spring:
  datasource:
    url: jdbc:mysql://mysql:3306/category_db?createDatabaseIfNotExist=true
    username: root
    password: ""

eureka:
  instance:
    prefer-ip-address: ${EUREKA_PREFER_IP:true}
allowed:
  origin: ${ALLOWED_ORIGIN:http://localhost:8080}
