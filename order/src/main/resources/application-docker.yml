spring:
  datasource:
    url: jdbc:mysql://mysql:3306/order_db?createDatabaseIfNotExist=true
    username: root
    password: ""

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

services:
  cart:
    baseUrl: http://cart/api/v1/carts
  inventory:
    baseUrl: http://inventory/api/v1/inventories
